# Auto-Generated Fixed Dockerfile
# Generated by Docker Security Scanner
# Original image: nginx:1.18

# SECURITY FIX: Upgraded base image
# Original: nginx:1.18
# Fixed:    nginx:1.25
FROM nginx:1.25

LABEL maintainer="security-team"
LABEL security.scan="passed"
LABEL security.base-image="nginx:1.25"

# SECURITY FIX: Update vulnerable packages
# Fixing 58 vulnerable package(s)

# Manual package updates may be required
# Check package manager documentation for your base image

# SECURITY: Create non-root user
RUN groupadd -r appuser && \
    useradd -r -g appuser appuser && \
    mkdir -p /app && \
    chown -R appuser:appuser /app

WORKDIR /app

# TODO: Copy your application files
# COPY --chown=appuser:appuser . /app

# SECURITY: Run as non-root user
USER appuser

# TODO: Add healthcheck for your application
# HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
#   CMD curl -f http://localhost/ || exit 1

# TODO: Set your application command
# CMD ["your-command"]